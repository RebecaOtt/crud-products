spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/products-jpa
    username: postgres
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    generate-ddl: true